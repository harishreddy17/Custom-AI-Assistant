Here's a simple code snippet in Python to add two numbers:

```python
# Function to add two numbers
def add_numbers(a, b):
    return a + b

# Example usage
num1 = 5
num2 = 7
result = add_numbers(num1, num2)
print(f"The sum of {num1} and {num2} is: {result}")
```

### Explanation:
1. **Function Definition**: The `add_numbers` function takes two parameters `a` and `b` and returns their sum.
2. **Example Usage**: We define two numbers (`num1` and `num2`), call the function with these numbers, and print the result.

### Output:
```
The sum of 5 and 7 is: 12
```

### Other Languages:
If you need the code in another language, let me know! For example, here's how you could do it in JavaScript:

```javascript
// Function to add two numbers
function addNumbers(a, b) {
    return a + b;
}

// Example usage
let num1 = 5;
let num2 = 7;
let result = addNumbers(num1, num2);
console.log(`The sum of ${num1} and ${num2} is: ${result}`);
```